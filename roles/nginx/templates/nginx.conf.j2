http {
    server {
        listen 80;
        server_name {{ inventory_hostname }}.ru-central1.internal;

        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
        }
    }
}
